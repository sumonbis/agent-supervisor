{
  "name": "@agentsafe/core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared model for AI-agent supervision & safety: SSM, trace schema, reference oracles, scoring, and the SupervisionReport contract.",
  "license": "MIT",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "compile": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --test dist/test/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.4.5"
  }
}
